# This project currently uses only the Python standard library at runtime.
# The only external runtime dependency is the `exiftool` executable, which is
# installed outside of pip.
#
# On Synology NAS, you do not need to `pip install -r requirements.txt`
# unless this file grows in the future.
